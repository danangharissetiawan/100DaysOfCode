void main() {
  // the Boolean data type recognizes only the literal true as true. Any other value is considered as false. Consider the following example −
  var str = "abcd";
  if (str == 'abcd') {
    // if str only error
    print('String is not empty');
  } else {
    print('Empty String');
  }
}
